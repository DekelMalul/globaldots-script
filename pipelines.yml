resources:
  - name: globaldot_script
    type: GitRepo
    configuration:
      gitProvider: github       
      path: DekelMalul/globaldot-script  
     
      
pipelines:
  - name: build_image
    steps:
      - name: build
        type: DockerBuild 
        configuration:
          inputResources:
            - name: globaldot_script
          dockerFileLocation: dockerfile/
          dockerFileName: dockerfile
          dockerImageName: strupper
          dockerImageTag: latest
