resources:
  - name: globaldots_script
    type: GitRepo
    configuration:
      gitProvider: github       
      path: DekelMalul/globaldots-script  
     
      
pipelines:
  - name: build_image
    steps:
      - name: build
        type: DockerBuild 
        configuration:
          inputResources:
            - name: globaldots_script
          dockerFileLocation: dockerfile/
          dockerFileName: dockerfile
          dockerImageName: strupper
          dockerImageTag: latest
