resources:
  - name: globaldot_script
    type: GitRepo
    configuration:
      gitProvider: github       
      path: DekelMalul/globaldots-scripts  
     
      
pipelines:
  - name: build_image
    steps:
      - name: build
        type: DockerBuild 
        configuration:
          inputResources:
            - name: globaldot
          dockerFileLocation: dockerfile/
          dockerFileName: dockerfile
          dockerImageName: strupper
          dockerImageTag: latest
